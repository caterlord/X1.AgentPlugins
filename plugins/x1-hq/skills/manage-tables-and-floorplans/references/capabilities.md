# Generated table and floorplan capabilities

<!-- Generated by scripts/validate-agent-plugin.mjs. Do not edit by hand. -->

Canonical contract digest: `98010ef605023960ac725cc10494db9d0f0c3c6c5b54f5686d3eb22b42fc8b15`

The connected gateway remains authoritative at runtime. A tool can be hidden or rejected by scopes, tenant policy, rollout flags, or a circuit breaker even when it appears in this release reference.

| Tool | Mode | Required scope | Schema | Stability | Maturity | Purpose |
| --- | --- | --- | --- | --- | --- | --- |
| `bootstrap_hq_workspace` | commit | `settings.read` | 1.0.0 | draft | active | Start an authenticated X1 HQ connection by resolving the user's workspace and optional company, brand, or shop names. Selects and persists a unique match; otherwise returns the shortest candidate list the user must disambiguate. Call this before any non-bootstrap HQ tool. |
| `call_hq_commit_tool` | commit | `settings.read` | 1.0.0 | beta | active | Execute one enabled commit tool returned by find_hq_tools only after its ordinary preview and explicit approval requirements are satisfied. The gateway validates the exact target schema and preserves the target tool's scope, tenant, approval, idempotency, task, readback, and audit rules. |
| `call_hq_preview_tool` | preview | `settings.read` | 1.0.0 | beta | active | Execute one enabled non-mutating preview tool returned by find_hq_tools. The gateway validates arguments against that tool's exact schema and enforces its original delegated scope, tenant boundary, quota, persistence, and audit rules. |
| `call_hq_read_tool` | read | `settings.read` | 1.0.0 | beta | read-only | Execute one enabled read tool returned by find_hq_tools. The gateway validates arguments against that tool's exact schema and enforces its original delegated scope and tenant boundary before execution. |
| `commit_clear_hq_workspace_selection` | commit | `settings.read` | 1.0.0 | draft | active | Clear the persisted MCP workspace selection for this authenticated assistant connection. |
| `commit_select_hq_business_scope` | commit | `settings.read` | 1.0.0 | draft | active | Persist or change the default HQ company/brand/shop scope inside the already selected workspace. |
| `commit_set_hq_workspace` | commit | `settings.read` | 1.0.0 | draft | active | Persist the selected HQ workspace for this authenticated assistant connection. workspaceId is the Clerk organization/workspace id; optional companyId/brandId/shopId set a default business scope inside that workspace. |
| `commit_table_settings` | commit | `table_settings.write` | 1.0.0 | beta | active | Commit an unchanged table-settings preview after explicit approval. Set approvalToken=user_explicitly_approved_final_preview only after the user approves the exact preview. Executes immediately, rejects stale target state and reads back the result. Preserve the idempotency key on retry. |
| `commit_takeaway_service_charge_settings` | commit | `table_settings.write` | 1.0.0 | beta | active | Commit an unchanged preview after explicit confirmation of the brand-wide change. Set approvalToken=user_explicitly_approved_final_preview only after approval. Rejects stale state and verifies by readback. Keep the idempotency key on retries. |
| `find_hq_tools` | read | `settings.read` | 1.1.0 | beta | read-only | maxResults must be 1–10. Reuse discovered schemas during this connection. Prefer schemaDetail=summary for broad searches, then schemaDetail=input with an exact tool name. Search the complete enabled X1 HQ MCP catalog for tools relevant to the current task and return their exact current schemas. Use this when a task-specific tool is not already callable, then invoke the selected result through the matching call_hq_read_tool, call_hq_preview_tool, or call_hq_commit_tool dispatcher. |
| `get_accessible_hq_scope` | read | `settings.read` | 1.1.0 | draft | read-only | List the companies, brands, and shops the authenticated HQ user can access inside the active Clerk workspace, with optional name filters for resolving brand and shop identifiers. |
| `get_hq_workspace_selection` | read | `settings.read` | 1.0.0 | draft | read-only | Return the current MCP workspace selection for this authenticated assistant connection. |
| `get_mcp_gateway_status` | read | `settings.read` | 1.2.1 | draft | read-only | Return the safe operator status, circuit-breaker impact, policy epoch, and schema marker for the current action list. |
| `get_table_settings_context` | read | `table_settings.read` | 1.0.0 | beta | read-only | Read shop parent tables (including recently removed), split suffixes, sections, floorplan backgrounds and table type/printer metadata. No QR tokens are returned. |
| `get_takeaway_service_charge_settings` | read | `table_settings.read` | 1.0.0 | beta | read-only | Read the brand-wide Takeaway table-type service-charge bypass. Applies across every shop in the brand, not to an individual service charge. |
| `get_task_status` | read | `settings.read` | 1.1.0 | draft | read-only | Return task status and verified outcome. Use waitMs=10000 while queued/running to avoid rapid polling; includeResult=false omits large results when only checking progress. |
| `list_hq_workspace_options` | read | `settings.read` | 1.0.0 | draft | read-only | List accessible HQ workspaces, each with companies, brands, and shops, plus the current MCP workspace selection. |
| `preview_table_settings` | preview | `table_settings.preview` | 1.0.0 | beta | active | Preview one table creation, partial edit, removal, restoration, section creation or shop section layout edit. Read current state first. Creation requires an explicit split choice. Multiple previews are separate non-atomic changes; create sections before tables and use returned IDs. |
| `preview_takeaway_service_charge_settings` | preview | `table_settings.preview` | 1.0.0 | beta | active | Preview changing the brand-wide Takeaway table-type service-charge bypass. Show the brand scope and before/after value for explicit confirmation before committing. |
| `resolve_hq_scope_reference` | read | `menu.read` | 1.0.0 | draft | read-only | Resolve company, brand, and shop identifiers from human-readable names and return disambiguation hints for no-ID workflows. |
| `retry_task` | commit | `settings.read` | 1.0.0 | draft | active | Retry one terminal failed asynchronous task that reports canRetry=true. The gateway preserves the original approved input, idempotency lineage, requester, tenant boundary, and required business-write scope, and returns the accepted retry task without creating a replacement operation. |
